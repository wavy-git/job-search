# Composing

Workflow for producing written application materials beyond the resume — cover letters, application essays, future written submissions (thank-you notes, addenda).

**Status: placeholder.** Folder exists. Content empty until the feature is built. Triggered when a JD requires a cover letter or application essay.

---

## Inclusion test

> **PASSES IF:** workflow, format, template, or exemplar for producing cover letters, application essays, or other written application submissions from `me/background.md` + `me/voice.md` + JD as inputs.
>
> **FAILS IF:** resume content or tailoring (→ `skills/tailoring/` + `resumes/`); DM / email / invite outreach (→ `skills/outreach/`); interview prep (→ `skills/interviewing/`); LinkedIn profile content (→ `me/linkedin.md`); facts about Maz (→ `me/background.md`); voice rules (→ `me/voice.md`); JD itself (→ `outputs/jds/`).

---

## Inputs (when built)

- `me/background.md` — facts to draw from
- `me/voice.md` — universal writing-style + claim rules + bridges
- The JD
- `me/resume.md` — for consistency with resume framing

## Outputs (when built)

- Cover letter `.md` or `.docx`
- Application essay `.md` or `.docx`
- Logged in `applications.md` row (essay text or reference)

## Notes

Two existing applications had essay submissions captured in `_archive/2026-05-20-rebuild/applications.md` notes: Customer.io ("Why this position" 3-part essay) and ElectronX ("Why ElectronX?" + "What's a product you wish you built?" reusing scent answer). These can be reference exemplars when this skill gets built.
